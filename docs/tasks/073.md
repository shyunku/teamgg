# #73 숙련도 numeric 직접 쓰기 전환

## 상태

| 항목 | 값 |
|---|---|
| 상태 | 🟡 WIP |
| 태그 | `backend` |
| 생성 | 2026-09-02 02:41 |
| 선행 작업 | #64 |

## 현재 진행

- 2026-09-03 16:10 KST — 기존 `masteries` 쓰기와 legacy→numeric 동기화 trigger 구조를 확인하고 직접 쓰기 전환 설계를 시작했다.
- 2026-09-03 16:10 KST — `MASTERY_WRITE_SOURCE`를 추가하고 선택한 원본 우선 쓰기, 반대 저장소의 동일 트랜잭션 mirror, authoritative Riot snapshot 기준 stale 행 삭제를 구현했다.
- 2026-09-03 16:10 KST — backend 중단 확인값을 요구하는 `20260903_001` 마이그레이션으로 기존 legacy→numeric shadow trigger를 제거하도록 구성했다. 운영 DB에는 아직 적용하지 않았다.
- 2026-09-03 16:24 KST — 전체 Go 테스트, vet, build, 대상 race test와 `git diff --check`를 통과했다. 실제 MySQL 통합 테스트는 로컬 Docker가 꺼져 있어 컴파일만 확인하고 실행은 skip됐다.

## 목적

숙련도 쓰기 원본을 legacy `masteries`에서 `masteries_numeric_v2`로 전환해 legacy 테이블 제거를 준비한다.

## 완료 조건

- 신규·갱신·삭제가 numeric 테이블을 원본으로 처리
- 관찰 기간 동안 checksum·행 수 정합성 유지
- legacy rollback 또는 제한된 역동기화 경로 검증
- Riot 갱신·DataExplorer·통계 회귀 검증

## 전환·롤백

| 구간 | 읽기 | 쓰기 원본 | 보조 저장 |
|---|---|---|---|
| 배포 직후 | `numeric_v2` | `legacy` | 애플리케이션이 `numeric_v2` mirror |
| 관찰 | `numeric_v2` | `numeric_v2` | 같은 트랜잭션으로 legacy mirror |
| 롤백 | `legacy` | `legacy` | 같은 트랜잭션으로 `numeric_v2` mirror |

- 운영 적용 전 backend 중단과 사용자 확인이 필요하다.
- legacy 삭제는 #74 범위이며 #73에서는 실행하지 않는다.

## 다음 단계

1. 변경을 커밋·푸시한다.
2. 운영 backend를 중단하고 `MASTERY_WRITE_CUTOVER_OFFLINE_ACK=true`를 일회성으로 전달해 마이그레이션한다.
3. `MASTERY_READ_SOURCE=numeric_v2`, `MASTERY_WRITE_SOURCE=numeric_v2`로 backend를 재생성한다.
4. 신규 Riot 숙련도 갱신 후 양쪽 행·payload 정합성, dirty 통계 반영, API와 DataExplorer 오류를 관찰한다.
