# #76 오래된 경기 자동 정리

## 상태

| 항목 | 값 |
|---|---|
| 상태 | 🟡 WIP |
| 태그 | `backend` |
| 등록 | 2026-09-02 16:17 |
| 마지막 업데이트 | 2026-09-24 17:20 |
| 선행 작업 | #66 |

## 목적

#66에서 확정한 최신 패치 보존 정책을 운영자가 매번 수동 실행하지 않아도 안전하게 주기 적용한다.

## 완료 조건

- [ ] 기본 비활성이며 환경변수로 실행 주기와 유지 패치 수를 설정할 수 있다.
- [ ] 중복 실행을 막는 단일 실행 lock과 명시적인 maintenance window를 적용한다.
- [ ] dry-run으로 대상과 안전 조건을 확인한 뒤 백엔드를 중단하고, 성공·실패와 무관하게 복구한다.
- [ ] 디스크 하한, DB 부하, batch timeout, 전체 실행 제한을 넘으면 자동 중단한다.
- [ ] 처리량·잔여량·삭제 행·소요 시간·중단 사유를 구조화 로그와 운영 알림으로 남긴다.
- [ ] 단위·통합 테스트와 운영 dry-run 및 제한된 실제 실행으로 재시작 안전성을 검증한다.

## 초기 방향

- 기본 주기: 월 1회
- 실행 방식: 기존 `cleanup-retention` 명령 재사용
- 숫자 match/participant identity 매핑은 보존

## 현재 구현·검증

| 범위 | 결과 |
|---|---|
| 호스트 스케줄러 | 기본 비활성, KST 유지보수 시간대, 30일 주기·24시간 재시도, 파일 잠금 적용 |
| 삭제 안전장치 | dry-run 대상 확인 후에만 백엔드 중단, 디스크 하한·DB 스레드/락 대기 가드, 배치/전체 시간 제한, 삭제 후 백엔드 복구 |
| 관측 | JSON 로그에 대상·처리·잔여 경기 수, 삭제 행, 소요 시간, 종료 이유 기록; 선택형 웹훅 오류 알림 |
| 검증 | Go 전체 테스트, Python 스케줄러 단위 테스트 6개, 격리된 MySQL 8.0 dry-run·삭제 통합 테스트 통과 |

운영 검증: 2026-09-24 17:12~17:20 KST에 Linux 호스트 preview-only, 운영 dry-run 및 백엔드 중단 후 제한된 no-op 실제 명령을 검증했다. 최신 8개 패치만 존재해 대상·삭제가 모두 0건이었다. 백엔드는 healthy와 통계 API 200으로 복구했다. 상세 수치와 중단/복구 기록은 [운영 검증 보고서](../reports/2026-09-24-retention-production-verification.md)에 있다.

미완료: 만료 패치의 실제 행 삭제·중간 실패 후 재실행 경로는 운영 대상이 없어 미검증. 월간 cron과 삭제 활성화는 하지 않았다. 이 조건이 확인될 때까지 WIP를 유지한다.
