# #74 Legacy 숙련도 제거

## 상태

| 항목 | 값 |
|---|---|
| 상태 | 🟢 DONE |
| 태그 | `backend` |
| 생성 | 2026-09-02 02:41 |
| 완료 | 2026-09-04 10:20 |
| 선행 작업 | #73 |

## 목적

숙련도 운영 경로를 `masteries_numeric_v2`로 단일화하고 중복 legacy `masteries`를 제거해 디스크를 회수한다. 숙련도는 재수집 가능한 데이터라는 사용자 판단에 따라 전체 백업은 생략했다.

## 최종 결과

| 지표 | 적용 전 | 적용 후 |
|---|---:|---:|
| Legacy 테이블 | `masteries` 존재 | 제거 |
| 숙련도 통계 trigger 대상 | `masteries` | `masteries_numeric_v2` 3개 |
| 루트 디스크 사용량 | 116GB / 128GB (91%) | 89GB / 128GB (70%) |
| 루트 디스크 여유 | 13GB | 40GB |
| Legacy 파일 | 25.56GiB | 제거 |
| Numeric 파일 | 6.57GiB | 6.57GiB |

## 구현

- 런타임 읽기·쓰기·통계·DataExplorer 지표를 numeric 전용으로 변경했다.
- 이중 운영 승인과 backend 중지를 요구하는 `20260903_002` 마이그레이션을 추가했다.
- 통계 trigger를 numeric 테이블로 이전한 뒤 legacy 테이블을 제거했다.
- 폐기된 `MASTERY_READ_SOURCE`, `MASTERY_WRITE_SOURCE` 설정을 운영 env에서 제거했다.
- MySQL 통합 테스트의 임시 DB 정리 순서를 수정해 테스트 DB 잔여 0건을 확인했다.

## 검증

- 삭제 전 최근 numeric ID 100명 표본: 737행, legacy/numeric 행 수·checksum 일치
- 삭제 전 legacy 참조 FK 0건, 활성 DB lock wait 0건
- 격리 MySQL 통합 테스트 2회 통과: 2.82초, 1.61초
- 마이그레이션: `20260903_002`, `dirty=0`, 686ms
- 적용 후 legacy 테이블·trigger·파일 0건, numeric 통계 trigger 3건
- 실제 `/v1/renewSummoner`: HTTP 200, 1.05초, numeric mastery 170행 유지
- `/`, 챔피언 통계, 메타 통계 API HTTP 200 및 backend healthy
- 적용 후 관련 ERROR·panic·deadlock·missing destination 로그 없음

## 관련 커밋

- `00fdfa8` — legacy 숙련도 저장소 제거 구현
- `d2c1308` — MySQL 통합 테스트 실행 지원
- `6079056` — MySQL 테스트 임시 DB 정리 수정
