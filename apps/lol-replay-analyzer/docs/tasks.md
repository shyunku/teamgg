# 리플레이 분석기 작업 목록

## WIP

- 진행 중인 항목 없음.

## TODO

- [ ] Docker Desktop 엔진을 켠 환경에서 `docker compose --env-file .env.compose --profile development build`와 세 서비스 smoke test를 실행한다.
- [ ] **최우선: 배포 환경에서 공유 리플레이 분석 수동 통합 검증**
  - 방장 계정으로 작업 생성과 직접 ROFL 업로드를 확인한다.
  - 참여자 계정에서 동일 작업의 단계별 진행률과 완료 결과가 공유되는지 확인한다.
  - 참여자는 업로드·삭제가 거부되고 방장만 완료/실패 내역을 삭제할 수 있는지 확인한다.
  - 동일 내전 중복 업로드, 만료 티켓, 재사용 티켓, 분석 서버 중단 후 오래된 작업 복구를 확인한다.
  - 실제 OpenAI 호출 전 모델·호스트·전송 데이터·예상 비용을 설명하고 사용자 승인을 받는다.
- [ ] `rofl-parser`에서 시간대별 개인/팀 골드와 경험치를 구조화하면 팀 골드 곡선·우위 역전을 실제 사건 점수에 활성화한다.
- [ ] 중립 오브젝트 스틸 여부를 사건별로 확정할 수 있는 팀·강타 근거를 추가한다.
- [ ] `detectorWardNetId`가 디코딩되면 시야 진입·이탈과 실제 와드를 연결한다.
- [ ] 10초 이동 샘플보다 정밀한 합류 시간과 4대5 상태 계산을 추가한다.
- [ ] 최종 AI 출력용 대안 행동·개인 피드백 응답 스키마를 구현한다.
- [ ] 사용자 승인 후 `06-event-dossiers.json`을 실제 OpenAI 분석 입력에 연결한다.
- [ ] 보존된 디코딩 결과를 쓰는 fixture 기반 통합 테스트를 추가한다.
- [ ] `rofl-parser`의 decoder artifact ESM/CommonJS 경계를 라이브러리에서 해결한다.

## DONE

- [x] 백엔드·프론트엔드·분석 서버용 multi-stage Dockerfile과 `.dockerignore`를 추가했다.
- [x] 기본 실행은 서버 2개, `development` 프로필은 프론트까지 3개가 뜨는 루트 Docker Compose를 구성했다.
- [x] 백엔드와 분석 서버의 Docker 환경파일을 분리하고 DB/JWT와 OpenAI 비밀값이 교차 노출되지 않도록 구성했다.
- [x] 백엔드가 `.env` 파일 없이 process environment로 기동하고 Redis 주소를 환경변수로 받도록 수정했다.
- [x] Compose 렌더링·환경변수 격리와 분석 서버 production 시작 경로를 검증했다.
- [x] team.gg 공유 분석 작업을 MySQL에 저장하고 방장·참여자의 목록, 진행률, Markdown 결과 조회 권한을 분리했다.
- [x] 동일 내전의 실행 중 작업을 하나로 제한하고 DB 행 잠금·상태 전이·request ID 검증으로 중복 분석을 차단했다.
- [x] 분석 서버 응답이 끊긴 오래된 작업을 기본 45분 후 실패 처리해 새 업로드가 가능하게 했다.
- [x] 브라우저가 ROFL을 team.gg 백엔드를 거치지 않고 분석 서버로 직접 업로드하도록 연결했다.
- [x] 업로드 티켓을 URL 대신 `X-Replay-Upload-Ticket` 헤더로 전달하고 CORS preflight 회귀 테스트를 추가했다.
- [x] 분석 서버 진행 콜백은 최대 초당 1회로 병합하고 완료·실패 콜백은 장시간 재시도하도록 보강했다.
- [x] 공유 분석 연동 후 백엔드 전체 테스트, 분석 서버 테스트·타입 검사·빌드, 클라이언트 프로덕션 빌드를 통과했다.
- [x] team.gg 방장이 생성한 서명 업로드 티켓을 검증하고 해당 내전의 공유 분석 작업으로 직접 업로드한다.
- [x] 업로드·디코딩·정제·프롬프트 구성·AI 분석 진행률을 team.gg 백엔드에 최대 초당 1회 전달한다.
- [x] 완료 Markdown과 모델, 실패 원인을 team.gg 공유 분석 내역에 저장한다.
- [x] 업로드 이후 서버 분석 단계를 SSE 전체 진행률로 정규화하고 클라이언트 진행 UI에 연결했다.
- [x] `KR-8326522247.rofl`을 `rofl-parser@0.4.0`으로 디코딩했다.
- [x] ROFL 종료 통계에서 플레이어·팀 요약을 자동 정제한다.
- [x] `paramHint` 하위 16비트로 `p1`~`p10` 리플레이 내부 ID를 부여한다.
- [x] `refined/metadata.json`과 압축 테이블 형식의 정제 패킷을 생성한다.
- [x] 모든 패킷을 순회해 parse level 0 초과 및 `namedParameters`가 있는 후보를 자동 보존한다.
- [x] 킬러·피해자, 이동, 스킬, 피해, 아이템, 와드, 체력, 구조물 이벤트를 정제한다.
- [x] 용·전령·바론 생성과 사망을 entity NetId로 연결해 중립 오브젝트 처치를 정제한다.
- [x] 플레이어 시야 진입·이탈을 정제하고 사건별로 요약한다.
- [x] 킬·구조물·중립 오브젝트를 하나의 사건 후보 점수 체계로 병합한다.
- [x] 사건 시작·종료 위치와 체력, 사건 중심 주변 인원, 와드, 시야, 피해, 스킬 근거를 생성한다.
- [x] 사건 영향도와 데이터·해석 신뢰도를 분리해 계산한다.
- [x] 골드 시계열 및 우위 역전의 영향도 계산 로직과 테스트를 구현했다.
- [x] 골드·경험치 원천이 없을 때 가짜 추정 대신 명시적 `unavailable`을 출력한다.
- [x] AI 직전 입력을 `teamgg-ai-event-input-v3`으로 확정하고 `process/prompt-assets/06-event-dossiers.json`에 저장한다.
- [x] 사건별 필수 근거 충족률과 전체 `readyForAi` 검증을 자동 생성한다.
- [x] 샘플 기준 5개 사건 모두 위치·체력·전투·시야 필수 검증을 통과했다.
- [x] 샘플 최종 입력은 minified 35,185자로 축소됐고 첫 실행 메타데이터 생성 순서도 검증했다.
- [x] 영향도 및 골드 우위 역전 계산 단위 테스트를 추가했다.
- [x] 기본 분석 뒤 작업 폴더를 삭제하고 `--keep-artifacts` 또는 API `keepArtifacts=true`에서만 보존한다.
- [x] Fastify 응답 생명주기를 우회하는 SSE 응답에도 허용된 CORS Origin 헤더를 직접 추가했다.
